name: CI/CD Pipeline

on:
  push:
    branches:
      - main # Change this to the branch you want to trigger builds on
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout the target repository
        uses: actions/checkout@v2
        with:
          repository: MithunTechnologiesDevOps/spring-boot-mongo-docker

      - name: Set up JDK
        uses: actions/setup-java@v2
        with:
          java-version: '8' # Specify the Java version
          distribution: 'adopt' # Specify the JDK distribution

      - name: Build with Maven
        run: |
          cd spring-boot-mongo-docker # Navigate to the cloned repo directory
          mvn clean package -DskipTests

      - name: Build Docker image
        run: |
          cd spring-boot-mongo-docker # Navigate to the cloned repo directory
          docker build -t spring-boot-mongo-app .

      - name: Run Docker container
        run: |
          docker run -d -p 8080:8080 spring-boot-mongo-app
